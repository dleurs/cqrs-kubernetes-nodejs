# Next, curl -H "Host: cqrs-dispatcher.default.example.com" http://6ddrucefok.lb.c1.gra7.k8s.ovh.net/order
apiVersion: serving.knative.dev/v1 # Current version of Knative
kind: Service
metadata:
  name: cqrs-command-order # The name of the app
  namespace: default # The namespace the app will use
  labels:
    serving.knative.dev/visibility: cluster-local
spec:
  template:
    spec:
      containers:
        - image: dleurs/cqrs-command-order:1.0.0 # Reference to the image of the app
          #ports: => reserved in knative
          #- containerPort: 8080
          env:
            #- name: PORT
            #  value: "8080"
            #- name: NODE_ENV
            #  value: "production" => Not working with prod
            - name: ORDERDBURL
              value: "http://cqrs-order-db.default.svc.cluster.local"
            #- name: QUERYORDERBURL
            #  value: "http://cqrs-query-order.default.svc.cluster.local"